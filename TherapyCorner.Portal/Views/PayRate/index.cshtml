@using TherapyCorner.Portal.Models
@using TherapyCorner.Portal.ResourceText;
@using www.therapycorner.com.company;

@model PayRateList

@{
    ViewBag.Title = Dictionary.PayRates;
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2 class="breadcrumb">@Html.ActionLink(Dictionary.Administration, "index") &gt; @Dictionary.PayRates</h2>

<section style="padding-bottom:10px">
    <button onclick="window.location='@Url.Action("create")'">@PayRatePages.AddRate</button>
</section>

<section>
    <table class="sortable">
        <thead>
            <tr>
                <th class="tdSingleButton"></th>
                <th style="min-width:150px"><a href="#">@Dictionary.Name</a></th>
               <th class="NumberText"><a href="#">@www.therapycorner.com.company.ResStrings.Dictionary.UnitRate</a></th>
                <th class="BoolText"><a href="#">@www.therapycorner.com.company.ResStrings.Dictionary.IsActive</a></th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Count > 0)
            {
                foreach (var p in Model)
                {
                    <tr>
                        <td>
                            <div class="tdSingleButton">

                                    <a href="@Url.Action("update",new { id = p.RateId })">
                                        <img src="~/images/edit.png" />
                                    </a>
                                
                            </div>
                        </td>
                        <td>@p.Name</td>
                        <td class="NumberText">@p.UnitRate.GetValueOrDefault(0).ToString("F2")</td>
                        <td>@(p.IsActive ? Dictionary.Yes : Dictionary.No)</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</section>
